import React from 'react';

const GoogleLogout = () => {
    return (
        <button>
            <a href="http://localhost:4500/auth/google/logout">Logout</a>
        </button>
    )
};

export default GoogleLogout;